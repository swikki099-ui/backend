<%- include('partials/head') %>

<div class="flex">
    <%- include('partials/sidebar') %>

    <main class="ml-64 flex-1 p-8 min-h-screen bg-gray-50">
        <div class="mb-8 flex items-center justify-between">
            <div>
                <h1 class="text-2xl font-bold text-gray-900">Dashboard</h1>
                <p class="text-gray-500 text-sm mt-1">ITS College — Overview & Analytics</p>
            </div>
            <div class="flex items-center gap-3">
                <span class="text-xs text-gray-400">v<%= appVersion || 'N/A' %></span>
                <span class="w-2 h-2 rounded-full bg-green-500 animate-pulse"></span>
                <span class="text-xs text-gray-500">Live</span>
            </div>
        </div>

        <!-- Row 1: Stats Cards -->
        <div class="grid grid-cols-1 md:grid-cols-2 xl:grid-cols-4 gap-5 mb-6">
            <div class="bg-white rounded-2xl p-6 shadow-sm border border-gray-100">
                <div class="flex items-center justify-between mb-3">
                    <div class="w-10 h-10 bg-blue-100 rounded-xl flex items-center justify-center">
                        <svg xmlns="http://www.w3.org/2000/svg" class="w-5 h-5 text-blue-600" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2"/><circle cx="9" cy="7" r="4"/><path d="M23 21v-2a4 4 0 0 0-3-3.87"/><path d="M16 3.13a4 4 0 0 1 0 7.75"/></svg>
                    </div>
                    <span class="text-xs font-medium text-blue-600 bg-blue-50 px-2 py-1 rounded-full"><%= stats.dau %> today</span>
                </div>
                <div class="text-3xl font-bold text-gray-900"><%= stats.totalUsers %></div>
                <div class="text-sm text-gray-500 mt-1">Total Students</div>
                <div class="mt-3 flex items-center gap-2 text-xs">
                    <div class="flex-1 bg-gray-100 rounded-full h-1.5">
                        <div class="bg-blue-500 h-1.5 rounded-full" style="width: <%= stats.profileRate %>%"></div>
                    </div>
                    <span class="text-gray-400 font-medium"><%= stats.profileRate %>% profile</span>
                </div>
            </div>

            <div class="bg-white rounded-2xl p-6 shadow-sm border border-gray-100">
                <div class="flex items-center justify-between mb-3">
                    <div class="w-10 h-10 bg-indigo-100 rounded-xl flex items-center justify-center">
                        <svg xmlns="http://www.w3.org/2000/svg" class="w-5 h-5 text-indigo-600" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><polyline points="22 12 18 12 15 21 9 3 6 12 2 12"/></svg>
                    </div>
                    <span class="text-xs font-medium text-indigo-600 bg-indigo-50 px-2 py-1 rounded-full">24h</span>
                </div>
                <div class="text-3xl font-bold text-gray-900"><%= stats.dau %></div>
                <div class="text-sm text-gray-500 mt-1">Daily Active Users</div>
            </div>

            <div class="bg-white rounded-2xl p-6 shadow-sm border border-gray-100">
                <div class="flex items-center justify-between mb-3">
                    <div class="w-10 h-10 bg-emerald-100 rounded-xl flex items-center justify-center">
                        <svg xmlns="http://www.w3.org/2000/svg" class="w-5 h-5 text-emerald-600" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M22 11.08V12a10 10 0 1 1-5.93-9.14"/><polyline points="22 4 12 14.01 9 11.01"/></svg>
                    </div>
                    <span class="text-xs font-medium text-emerald-600 bg-emerald-50 px-2 py-1 rounded-full"><%= stats.profileRate %>%</span>
                </div>
                <div class="text-3xl font-bold text-gray-900"><%= stats.profilesCompleted %></div>
                <div class="text-sm text-gray-500 mt-1">Profiles Complete</div>
                <div class="mt-3 bg-gray-100 rounded-full h-1.5">
                    <div class="bg-emerald-500 h-1.5 rounded-full" style="width: <%= stats.profileRate %>%"></div>
                </div>
            </div>

            <div class="bg-white rounded-2xl p-6 shadow-sm border border-gray-100">
                <div class="flex items-center justify-between mb-3">
                    <div class="w-10 h-10 bg-red-100 rounded-xl flex items-center justify-center">
                        <svg xmlns="http://www.w3.org/2000/svg" class="w-5 h-5 text-red-600" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="12" cy="12" r="10"/><line x1="4.93" y1="4.93" x2="19.07" y2="19.07"/></svg>
                    </div>
                    <% if (stats.activeBans > 0) { %>
                        <span class="text-xs font-medium text-red-600 bg-red-50 px-2 py-1 rounded-full">Active</span>
                    <% } %>
                </div>
                <div class="text-3xl font-bold text-gray-900"><%= stats.activeBans %></div>
                <div class="text-sm text-gray-500 mt-1">Banned Users</div>
            </div>
        </div>

        <!-- Row 2: Secondary Stats + Chart -->
        <div class="grid grid-cols-1 xl:grid-cols-4 gap-5 mb-6">
            <div class="bg-white rounded-2xl p-6 shadow-sm border border-gray-100">
                <div class="flex items-center gap-3 mb-2">
                    <div class="w-9 h-9 rounded-lg bg-purple-100 flex items-center justify-center">
                        <svg xmlns="http://www.w3.org/2000/svg" class="w-4 h-4 text-purple-600" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2"/><circle cx="9" cy="7" r="4"/></svg>
                    </div>
                    <div>
                        <div class="text-2xl font-bold text-gray-900"><%= stats.totalFaculty %></div>
                        <div class="text-xs text-gray-500">Faculty</div>
                    </div>
                </div>
            </div>
            <div class="bg-white rounded-2xl p-6 shadow-sm border border-gray-100">
                <div class="flex items-center gap-3 mb-2">
                    <div class="w-9 h-9 rounded-lg bg-amber-100 flex items-center justify-center">
                        <svg xmlns="http://www.w3.org/2000/svg" class="w-4 h-4 text-amber-600" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><rect x="3" y="4" width="18" height="18" rx="2" ry="2"/><line x1="16" y1="2" x2="16" y2="6"/><line x1="8" y1="2" x2="8" y2="6"/><line x1="3" y1="10" x2="21" y2="10"/></svg>
                    </div>
                    <div>
                        <div class="text-2xl font-bold text-gray-900"><%= stats.totalEvents %></div>
                        <div class="text-xs text-gray-500">Calendar Events</div>
                    </div>
                </div>
            </div>
            <div class="bg-white rounded-2xl p-6 shadow-sm border border-gray-100">
                <div class="flex items-center gap-3 mb-2">
                    <div class="w-9 h-9 rounded-lg bg-sky-100 flex items-center justify-center">
                        <svg xmlns="http://www.w3.org/2000/svg" class="w-4 h-4 text-sky-600" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M21 11.5a8.38 8.38 0 0 1-.9 3.8 8.5 8.5 0 0 1-7.6 4.7 8.38 8.38 0 0 1-3.8-.9L3 21l1.9-5.7a8.38 8.38 0 0 1-.9-3.8 8.5 8.5 0 0 1 4.7-7.6 8.38 8.38 0 0 1 3.8-.9h.5a8.48 8.48 0 0 1 8 8v.5z"/></svg>
                    </div>
                    <div>
                        <div class="text-2xl font-bold text-gray-900"><%= stats.totalPosts %></div>
                        <div class="text-xs text-gray-500">Social Posts</div>
                    </div>
                </div>
            </div>
            <div class="bg-white rounded-2xl p-6 shadow-sm border border-gray-100">
                <div class="flex items-center gap-3 mb-2">
                    <div class="w-9 h-9 rounded-lg bg-rose-100 flex items-center justify-center">
                        <svg xmlns="http://www.w3.org/2000/svg" class="w-4 h-4 text-rose-600" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"/></svg>
                    </div>
                    <div>
                        <div class="text-2xl font-bold text-gray-900"><%= stats.totalUsers - stats.profilesCompleted %></div>
                        <div class="text-xs text-gray-500">Incomplete</div>
                    </div>
                </div>
            </div>
        </div>

        <!-- Row 3: Main Content -->
        <div class="grid grid-cols-1 xl:grid-cols-3 gap-5">
            <!-- Recent Registrations -->
            <div class="xl:col-span-2 bg-white rounded-2xl shadow-sm border border-gray-100">
                <div class="flex items-center justify-between px-6 py-5 border-b border-gray-100">
                    <h2 class="font-semibold text-gray-900">Recent Registrations</h2>
                    <a href="/admin/users" class="text-blue-600 text-sm font-medium hover:underline">View All →</a>
                </div>
                <div class="overflow-x-auto">
                    <table class="w-full text-sm">
                        <thead>
                            <tr class="bg-gray-50">
                                <th class="text-left px-6 py-3 text-xs font-semibold text-gray-500 uppercase tracking-wider">Student</th>
                                <th class="text-left px-6 py-3 text-xs font-semibold text-gray-500 uppercase tracking-wider">Roll No</th>
                                <th class="text-left px-6 py-3 text-xs font-semibold text-gray-500 uppercase tracking-wider">Sem</th>
                                <th class="text-left px-6 py-3 text-xs font-semibold text-gray-500 uppercase tracking-wider">Course</th>
                                <th class="text-left px-6 py-3 text-xs font-semibold text-gray-500 uppercase tracking-wider">Joined</th>
                            </tr>
                        </thead>
                        <tbody class="divide-y divide-gray-50">
                            <% if (recentUsers.length === 0) { %>
                            <tr><td colspan="5" class="text-center text-gray-400 py-10">No users found</td></tr>
                            <% } %>
                            <% recentUsers.forEach(function(u) { %>
                            <tr class="hover:bg-gray-50 transition-colors">
                                <td class="px-6 py-4">
                                    <div class="flex items-center gap-3">
                                        <div class="w-8 h-8 rounded-full bg-gradient-to-br from-blue-400 to-indigo-600 flex items-center justify-center text-white text-xs font-bold flex-shrink-0">
                                            <%= u.name ? u.name.charAt(0).toUpperCase() : '?' %>
                                        </div>
                                        <div>
                                            <div class="font-medium text-gray-900"><%= u.name || '—' %></div>
                                            <div class="text-xs text-gray-400"><%= u.email || '—' %></div>
                                        </div>
                                    </div>
                                </td>
                                <td class="px-6 py-4 text-gray-700 font-mono text-xs"><%= u.roll_no || '—' %></td>
                                <td class="px-6 py-4"><span class="text-xs font-medium bg-gray-100 text-gray-700 px-2 py-0.5 rounded"><%= u.semester || '—' %></span></td>
                                <td class="px-6 py-4 text-gray-500 text-xs"><%= u.course || '—' %></td>
                                <td class="px-6 py-4 text-gray-400 text-xs"><%= u.created_at ? new Date(u.created_at).toLocaleDateString('en-IN') : '—' %></td>
                            </tr>
                            <% }); %>
                        </tbody>
                    </table>
                </div>
            </div>

            <!-- Right Column -->
            <div class="space-y-5">
                <!-- App Version -->
                <div class="bg-gradient-to-br from-blue-600 to-indigo-700 rounded-2xl p-6 text-white shadow-lg shadow-blue-500/25">
                    <div class="flex items-center gap-2 mb-3">
                        <svg xmlns="http://www.w3.org/2000/svg" class="w-5 h-5 opacity-80" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M12 2a10 10 0 1 0 10 10H12V2Z"/></svg>
                        <span class="font-semibold text-sm">App Version</span>
                    </div>
                    <div class="text-3xl font-bold mb-1"><%= appVersion || 'N/A' %></div>
                    <div class="text-blue-200 text-xs mb-4"><%= appForce ? '⚠ Force update enabled' : '✓ Optional update' %></div>
                    <a href="/admin/app-update" class="inline-flex items-center gap-2 bg-white/20 hover:bg-white/30 transition-colors text-white text-sm font-medium px-4 py-2 rounded-lg">
                        Manage Update
                        <svg xmlns="http://www.w3.org/2000/svg" class="w-3.5 h-3.5" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><line x1="5" y1="12" x2="19" y2="12"/><polyline points="12 5 19 12 12 19"/></svg>
                    </a>
                </div>

                <!-- Quick Actions -->
                <div class="bg-white rounded-2xl shadow-sm border border-gray-100 p-6">
                    <h2 class="font-semibold text-gray-900 mb-4">Quick Actions</h2>
                    <div class="space-y-2">
                        <a href="/admin/users" class="flex items-center gap-3 p-3 rounded-xl hover:bg-blue-50 group transition-colors">
                            <div class="w-8 h-8 rounded-lg bg-blue-100 flex items-center justify-center group-hover:bg-blue-200 transition-colors">
                                <svg xmlns="http://www.w3.org/2000/svg" class="w-4 h-4 text-blue-600" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2"/><circle cx="9" cy="7" r="4"/></svg>
                            </div>
                            <span class="text-sm text-gray-700 font-medium">Manage All Users</span>
                        </a>
                        <a href="/admin/announcements/new" class="flex items-center gap-3 p-3 rounded-xl hover:bg-green-50 group transition-colors">
                            <div class="w-8 h-8 rounded-lg bg-green-100 flex items-center justify-center group-hover:bg-green-200 transition-colors">
                                <svg xmlns="http://www.w3.org/2000/svg" class="w-4 h-4 text-green-600" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><line x1="12" y1="5" x2="12" y2="19"/><line x1="5" y1="12" x2="19" y2="12"/></svg>
                            </div>
                            <span class="text-sm text-gray-700 font-medium">New Announcement</span>
                        </a>
                        <a href="/admin/bans" class="flex items-center gap-3 p-3 rounded-xl hover:bg-red-50 group transition-colors">
                            <div class="w-8 h-8 rounded-lg bg-red-100 flex items-center justify-center group-hover:bg-red-200 transition-colors">
                                <svg xmlns="http://www.w3.org/2000/svg" class="w-4 h-4 text-red-600" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="12" cy="12" r="10"/><line x1="4.93" y1="4.93" x2="19.07" y2="19.07"/></svg>
                            </div>
                            <span class="text-sm text-gray-700 font-medium">View Banned Users</span>
                        </a>
                        <a href="/admin/calendar" class="flex items-center gap-3 p-3 rounded-xl hover:bg-purple-50 group transition-colors">
                            <div class="w-8 h-8 rounded-lg bg-purple-100 flex items-center justify-center group-hover:bg-purple-200 transition-colors">
                                <svg xmlns="http://www.w3.org/2000/svg" class="w-4 h-4 text-purple-600" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><rect x="3" y="4" width="18" height="18" rx="2" ry="2"/><line x1="16" y1="2" x2="16" y2="6"/><line x1="8" y1="2" x2="8" y2="6"/><line x1="3" y1="10" x2="21" y2="10"/></svg>
                            </div>
                            <span class="text-sm text-gray-700 font-medium">Manage Calendar</span>
                        </a>
                    </div>
                </div>
            </div>
        </div>

        <!-- Row 4: Live Feed -->
        <div class="mt-5 bg-white rounded-2xl shadow-sm border border-gray-100 overflow-hidden">
            <div class="flex items-center justify-between px-6 py-5 border-b border-gray-100 bg-gray-50/30">
                <div class="flex items-center gap-2">
                    <h2 class="font-semibold text-gray-900">Live Activity Feed</h2>
                    <span class="flex h-2 w-2">
                        <span class="animate-ping absolute inline-flex h-2 w-2 rounded-full bg-red-400 opacity-75"></span>
                        <span class="relative inline-flex rounded-full h-2 w-2 bg-red-500"></span>
                    </span>
                </div>
                <a href="/admin/activity" class="text-gray-400 hover:text-blue-600 transition-colors">
                    <svg xmlns="http://www.w3.org/2000/svg" class="w-4 h-4" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M18 13v6a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V8a2 2 0 0 1 2-2h6"/><polyline points="15 3 21 3 21 9"/><line x1="10" y1="14" x2="21" y2="3"/></svg>
                </a>
            </div>
            <div id="dashboard-activity-list" class="max-h-[300px] overflow-y-auto p-4 space-y-3">
                <div class="flex flex-col items-center justify-center h-full text-center p-6 text-gray-400">
                    <svg id="feed-spinner" class="animate-spin w-5 h-5 mb-2" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24"><circle class="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" stroke-width="4"/><path class="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4z"/></svg>
                    <p class="text-xs font-medium">Connecting to stream...</p>
                </div>
            </div>
        </div>
    </main>
</div>

<script src="https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2"></script>
<script>
    (function() {
        const supabaseLib = (typeof supabasejs !== 'undefined') ? supabasejs : supabase;
        const client = supabaseLib.createClient('<%= supabaseConfig.url %>', '<%= supabaseConfig.anonKey %>');
        const list = document.getElementById('dashboard-activity-list');
        
        async function loadInitial() {
            try {
                const { data, error } = await client.from('activity_feed').select('*').order('id', { ascending: false }).limit(10);
                if (data && data.length > 0) {
                    list.innerHTML = '';
                    data.forEach(item => appendItem(item, false));
                    if (window.lucide) lucide.createIcons();
                } else {
                    list.innerHTML = `<div class="p-6 text-center text-gray-400 text-[11px]">Waiting for new activity...</div>`;
                }
            } catch (e) {
                list.innerHTML = `<div class="p-6 text-center text-red-400 text-[11px]">Check SQL setup</div>`;
            }
        }

        function appendItem(item, animate = true) {
            const div = document.createElement('div');
            div.className = `flex items-start gap-3 p-3 rounded-xl border border-gray-50 transition-all ${animate ? 'animate-[slideIn_0.4s_ease-out] bg-blue-50/50 border-blue-100' : 'hover:bg-gray-50'}`;
            div.innerHTML = `
                <div class="w-8 h-8 rounded-lg flex items-center justify-center bg-${item.color}-100 text-${item.color}-600 flex-shrink-0">
                    <i data-lucide="${item.icon}" class="w-4 h-4"></i>
                </div>
                <div class="flex-1 min-w-0">
                    <div class="flex justify-between items-center mb-0.5">
                        <p class="text-[11px] font-bold text-gray-800 truncate">${item.title}</p>
                        <p class="text-[9px] text-gray-400 font-medium">${new Date(item.created_at).toLocaleTimeString([], {hour: '2-digit', minute:'2-digit'})}</p>
                    </div>
                    <p class="text-[10px] text-gray-500 line-clamp-1">${item.description}</p>
                </div>
            `;
            list.prepend(div);
            if (animate && window.lucide) lucide.createIcons();
            if (list.children.length > 15) list.removeChild(list.lastChild);
        }

        loadInitial();

        client.channel('dashboard-feed')
            .on('postgres_changes', { event: 'INSERT', schema: 'public', table: 'activity_feed' }, payload => {
                appendItem(payload.new, true);
            })
            .subscribe();
    })();
</script>

<style>
    @keyframes slideIn {
        0% { opacity: 0; transform: translateX(-10px); }
        100% { opacity: 1; transform: translateX(0); }
    }
</style>

</body>
</html>